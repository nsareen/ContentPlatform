# Compatible requirements for ContentPlatform - 2025-05-23
# This file contains specific versions of dependencies that are known to work together
# without conflicts between LangChain, LangGraph, and Pydantic.

# Core dependencies
fastapi==0.95.1
uvicorn==0.22.0
sqlalchemy==2.0.12
pydantic==1.10.7  # Important: Must be this version for compatibility with FastAPI 0.95.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
alembic==1.10.4
email-validator==2.0.0

# LangChain ecosystem - Compatible versions
langchain==0.0.267
langchain-core==0.1.22
langchain-community==0.0.20
langsmith==0.0.87
openapi-schema-pydantic==1.2.4
langgraph==0.0.17

# Environment and API
python-dotenv==1.0.0
openai==0.28.1
anthropic==0.5.0

# Notes:
# - Do not upgrade these packages independently as it may break compatibility
# - If you need to upgrade, test all components together
# - The main compatibility constraint is between:
#   * Pydantic 1.10.7 (required by FastAPI 0.95.1)
#   * LangChain 0.0.267
#   * LangGraph 0.0.17
#   * LangChain Core 0.1.22
